// three-react-fiber compoennt to render shapes with associated randomness in corrosponce with sound
// component to render a single pad accepting height width and samples data
import '../App.css';

function Shape() {
    return (
        <div className="pad">
            <button>Press me</button>
            {/*pad component*/}
        </div>
    );
}

export default Shape;
