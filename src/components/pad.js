// component to render a single pad accepting height width and samples data
import '../App.css';

function Pad() {
    return (
        <div className="pad">
            <button>Press me</button>
            {/*pad component*/}
        </div>
    );
}

export default Pad;
